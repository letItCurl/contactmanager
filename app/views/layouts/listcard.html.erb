<div class="card-header">
    <strong><%= yield(:title)%></strong>
</div>
<div>
    <%= yield(:content) %>
</div>
<div class="bg-light d-flex justify-content-center">
    <%= yield(:footer) %>   
</div>